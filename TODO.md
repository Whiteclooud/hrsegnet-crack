# TODO

本文档作为项目推进的唯一任务清单。任务完成后，从 Active 中删除；推进过程中发现的新任务，追加到 Active。

## Active

- [ ] 建立本轮推理结果评估清单。
  - 记录 B48/B32 在当前 16 张图上的主观效果。
  - 标记典型好样例、漏检样例、误检样例。
  - 优先查看 `outputs/rerun_16imgs_b48_tile400_overlap96/previews/` 和 `overlays/`。
- [ ] 做推理参数小实验。
  - 对比阈值：0.3、0.5、0.7。
  - 视效果决定是否尝试 `crop-size 512,512`、`overlap 128`。
- [ ] 增加结果汇总脚本。
  - 统计每张图预测裂缝像素占比。
  - 生成一个 CSV，便于快速筛选疑似裂缝图。
- [ ] 增加轻量后处理选项。
  - 支持去除小连通域。
  - 支持根据最小面积过滤明显噪声。
- [ ] 设计并实现裂缝长度测量流程。
  - 已新增第一版脚本：`scripts/measure_crack_length.py`。
  - 已新增说明文档：`docs/MEASUREMENT.md`。
  - 从 mask/prob 提取骨架中心线，输出像素级长度。
  - 读取 DJI 图片元数据，记录 GPS、相对高度、云台角度等信息。
  - 明确物理长度换算所需参数：相机内参/焦距、畸变、拍摄高度、坝面几何或标定点。
  - 当前第一版采用固定 GSD 近似换算，先查看粗略效果。
  - 已知相机信息：DJI M3TD ZoomCamera，4000x3000，Focal Length 29.9 mm，35mm 等效焦距 161 mm，RelativeAltitude 约 20.013 m，GimbalPitchDegree 约 -89.90。
  - 当前 GSD 初始估算值：`1.075 mm/px`。
  - 支持曲折裂缝、多分支裂缝和断裂小段的长度统计策略。
- [ ] 根据参数实验结果决定是否微调。
- [ ] 如需微调，制定标注方案。
  - 第一批建议 50-100 个代表性 tile。
  - 优先使用模型初始 mask 后人工修正，减少从零标注成本。

## Notes

- 本地无 GPU；服务器为 RTX 4090，24 GB 显存。
- 代码、配置、文档使用 Git 管理。
- 原始数据、模型权重、推理结果、训练输出不进入 Git。
- 推荐流程：本地改代码并提交，服务器拉取代码运行，结果再同步回本地查看。
