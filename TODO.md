# TODO

本文档作为项目推进的唯一任务清单。任务完成后，从 Active 中删除；推进过程中发现的新任务，追加到 Active。

## Active

- [ ] 建立本轮推理结果评估清单。
  - 记录 B48/B32 在当前 16 张图上的主观效果。
  - 标记典型好样例、漏检样例、误检样例。
  - 优先查看 `outputs/rerun_16imgs_b48_tile400_overlap96/previews/` 和 `overlays/`。
- [ ] 做推理参数小实验。
  - 对比阈值：0.3、0.5、0.7。
  - 视效果决定是否尝试 `crop-size 512,512`、`overlap 128`。
- [ ] 增加结果汇总脚本。
  - 统计每张图预测裂缝像素占比。
  - 生成一个 CSV，便于快速筛选疑似裂缝图。
- [ ] 增加轻量后处理选项。
  - 支持去除小连通域。
  - 支持根据最小面积过滤明显噪声。
- [ ] 根据参数实验结果决定是否微调。
- [ ] 如需微调，制定标注方案。
  - 第一批建议 50-100 个代表性 tile。
  - 优先使用模型初始 mask 后人工修正，减少从零标注成本。

## Notes

- 本地无 GPU；服务器为 RTX 4090，24 GB 显存。
- 代码、配置、文档使用 Git 管理。
- 原始数据、模型权重、推理结果、训练输出不进入 Git。
- 推荐流程：本地改代码并提交，服务器拉取代码运行，结果再同步回本地查看。
